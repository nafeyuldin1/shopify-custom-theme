<style>
  /* Base Image Style */
  .customstyle{{ section.id }} .collection-card__image{
    border-radius:{{ section.settings.radius }}%;
    isolation: isolate;
    width: 100%;
    height: auto;
    object-fit: cover;
    display: block;
  }
  
  /* Text Overlay Styling */
  .customstyle{{ section.id }} .collection-card-1-infor{
    width: 100%;
    text-align: center;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
    padding: 0 10px;
  }
  
  /* Heading Style */
  .customstyle{{ section.id }} .collection-card-1-infor h4 {
    color: #fff;
    font-weight: bold;
    font-size: 24px; /* Default size */
    margin: 0;
    line-height: 1.2;
    text-shadow: 0 2px 4px rgba(0,0,0,0.3);
  }

  /* --- DESKTOP LAYOUT --- */
  .customstyle{{ section.id }} .collectionlist-slider {
    display: flex;
    flex-wrap: wrap;
    margin: 0 -10px;
  }
  .customstyle{{ section.id }} .collectionlist-item {
    padding: 0 10px;
    margin-bottom: 20px;
    box-sizing: border-box;
    width: calc(100% / {{ section.settings.show }}); /* Desktop Width */
  }

  /* --- MOBILE LAYOUT (The Fix) --- */
  @media (max-width: 767px) {
    /* Force 2 items in one row strictly using CSS */
    .customstyle{{ section.id }} .collectionlist-item {
      /* Dynamic Width Calculation based on setting */
      width: calc(100% / {{ section.settings.show_mobile }}) !important;
      padding: 0 5px !important; /* Smaller gap for mobile */
      margin-bottom: 10px !important;
      flex: 0 0 auto !important;
    }
    
    /* Container adjustment for smaller gaps */
    .customstyle{{ section.id }} .collectionlist-slider {
        margin: 0 -5px !important;
    }

    /* Adjust font size on mobile so it fits */
    .customstyle{{ section.id }} .collection-card-1-infor h4 {
        font-size: 16px !important; 
    }
  }

  /* Slick Slider Helper */
  .collectionlist-slider.slick-initialized .slick-slide {
      display: block;
      height: auto;
  }
  
  /* Style Item Top Specifics */
  {% if section.settings.style_item == "top" %}
    .customstyle{{ section.id }} .button-collection-grid{
        display: none; /* Hide button in this style for cleaner look */
    }
  {% else %}
    .customstyle{{ section.id }} .collectionlist-item:hover h4{
        color: var(--g-main);
    }
  {% endif %}
</style>

<div class="customstyle{{ section.id }}" data-aos="{{ section.settings.animation }}">
  {%- render 'style-section' -%}
  <div class="{{ section.settings.setwidth }}">
    <div class="section-block">
      {%- render 'section-heading' -%}
      <div class="collectionlist-wrapper pt-0 pt-lg-0">
        
        <div class="collectionlist-slider" id="collection-list-{{ section.id }}"
            data-rows="{{ section.settings.rows }}"
            data-slidesToShow="{{ section.settings.show }}"
            data-slidestoscroll="{{ section.settings.show }}"
            data-arrows="{{ section.settings.arrows }}"
            data-dots="{{ section.settings.dots }}" 
            data-draggable="{{ section.settings.draggable }}" 
            data-infinite="{{ section.settings.infinite }}" 
            data-section-id="{{ section.id }}" 
            data-slidestoshow-mobile="{{ section.settings.show_mobile }}"
          >
          {%- for block in section.blocks -%}
          <div class="collectionlist-item carousel__slide-wrapper--{{ block.id }}" {{ block.shopify_attributes }}>
            {%- assign featured_collection = collections[block.settings.collection] -%}
            {%- assign featured_image = block.settings.image -%}
            {%- assign icon_image = block.settings.icon -%}
            {%- render 'collection-grid-item', featured_image: featured_image, icon_image: icon_image, featured_collection: featured_collection, column: 'none', size_image: section.settings.size_image, headinglist: section.settings.size_text, show_count: section.settings.show_count, button_text: section.settings.button_text -%}
          </div>
          {%- endfor -%}
        </div>
      </div>
      {%- if section.blocks.size == 0 -%}
      {%- render 'no-blocks' -%}
      {%- endif -%}
    </div>
  </div>
</div>

{% comment %} 
  JAVASCRIPT: Logic to sync Slider with CSS 
{% endcomment %}
{% if section.settings.enable_carousel %}
<script>
document.addEventListener("DOMContentLoaded", function() {
    function initCollectionSlick{{ section.id | replace: '-', '' }}() {
        var $slider = $('#collection-list-{{ section.id }}');
        
        if ($slider.length === 0) return;

        var rows = parseInt($slider.data('rows')) || 1;
        var desktopShow = parseInt($slider.data('slidestoshow')) || 3;
        var mobileShow = parseInt($slider.data('slidestoshow-mobile')) || 2; 
        var arrows = $slider.data('arrows');
        var dots = $slider.data('dots');
        var infinite = $slider.data('infinite');
        var draggable = $slider.data('draggable');

        if ($slider.hasClass('slick-initialized')) {
            $slider.slick('unslick');
        }

        $slider.slick({
            rows: rows,
            slidesToShow: desktopShow,
            slidesToScroll: 1,
            arrows: arrows,
            dots: dots,
            infinite: infinite,
            draggable: draggable,
            responsive: [
                {
                    breakpoint: 1024,
                    settings: {
                        slidesToShow: desktopShow >= 3 ? 3 : desktopShow,
                        slidesToScroll: 1
                    }
                },
                {
                    breakpoint: 768,
                    settings: {
                        slidesToShow: mobileShow, /* Ensures Slider Logic knows its 2 */
                        slidesToScroll: 1,
                        rows: 1
                    }
                }
            ]
        });
    }

    setTimeout(function(){
        if (typeof $ !== 'undefined') {
            initCollectionSlick{{ section.id | replace: '-', '' }}();
        }
    }, 200);

    if (Shopify.designMode) {
        document.addEventListener('shopify:section:load', function(event) {
            if (event.detail.sectionId === '{{ section.id }}') {
                initCollectionSlick{{ section.id | replace: '-', '' }}();
            }
        });
    }
});
</script>
{% endif %}

{% schema %}
{
  "name": "Collection list",
  "class": "index-section",
  "max_blocks": 6,
  "settings": [
    {
      "type": "select",
      "id": "animation",
      "options": [
        { "value": "", "label": "None" },
        { "value": "fade", "label": "Fade" },
        { "value": "slide-up", "label": "Slide Up" }
      ],
      "label": "Select Animation"
    },
    {
      "type": "select",
      "id": "setwidth",
      "label": "Full Width",
      "default": "container",
      "options": [
        { "value": "container", "label": "Box" },
        { "value": "container-fluid", "label": "Container Fluid" },
        { "value": "full", "label": "Full" }
      ]
    },
    {
      "type": "image_picker",
      "id":"bg_section",
      "label": "Background image"
    },
    {
      "type": "color",
      "id": "bgsection",
      "label": "Background Section",
      "default": "#ffffff"
    },
    {
      "type": "image_picker",
      "id": "image_header",
      "label": "Image header"
    },
    {
      "type": "text",
      "id": "title_top",
      "label": "Sub Title Top",
      "default": "Sub Title Top"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Collection list"
    },
    {
      "type": "textarea",
      "id": "des",
      "label": "HTML / Description"
    },
    {
      "type": "select",
      "id": "align-heading",
      "label": "Align Heading",
      "default": "center",
      "options": [
        { "value": "center", "label": "Center" },
        { "value": "left", "label": "Left" },
        { "value": "right", "label": "Right" }
      ]
    },
    {
      "type": "select",
      "id": "size_image",
      "label": "Size Image",
      "default": "auto",
      "options": [
        { "value": "auto", "label": "Natural" },
        { "value": "square", "label": "Square (1:1)" },
        { "value": "landscape", "label": "Landscape (4:3)" },
        { "value": "portrait", "label": "Portrait (2:3)" },
        { "value": "wide", "label": "Wide (16:9)" }
      ]
    },
    {
      "type": "select",
      "id": "style_item",
      "label": "Text index",
      "default": "bottom",
      "options": [
        { "value": "bottom", "label": "Bottom" },
        { "value": "top", "label": "Top" }
      ]
    },
    {
      "type": "select",
      "id": "rows",
      "label": "Rows",
      "default": "1",
      "options": [
        { "value": "1", "label": "1 row" },
        { "value": "2", "label": "2 rows" }
      ]
    },
    {
      "type": "range",
      "id": "show",
      "min": 2,
      "max": 6,
      "step": 1,
      "label": "Display items (Desktop)",
      "default": 3
    },
    {
      "type": "range",
      "id": "show_mobile",
      "min": 1,
      "max": 3,
      "step": 1,
      "label": "Display items (Mobile)",
      "default": 2
    },
    {
      "type": "range",
      "id": "radius",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "%",
      "label": "Border Radius",
      "default": 0
    },
    {
      "type": "range",
      "id": "space",
      "min": 10,
      "max": 80,
      "step": 5,
      "unit": "px",
      "label": "Spacing Column",
      "default": 15
    },
    {
      "type": "select",
      "id": "size_text",
      "default": "h4",
      "options": [
        { "value": "h1", "label": "H1" },
        { "value": "h2", "label": "H2" },
        { "value": "h3", "label": "H3" },
        { "value": "h4", "label": "H4" },
        { "value": "h5", "label": "H5" },
        { "value": "h6", "label": "H6" }
      ],
      "label": "Name Collection Size"
    },
    {
      "type": "checkbox",
      "id": "show_count",
      "label": "Show count",
      "default": true
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "info": "Leave blank to hide"
    },
    {
      "type": "checkbox",
      "id": "enable_carousel",
      "label": "Enable Carousel",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "arrows",
      "label": "Arrows",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "draggable",
      "label": "Draggable",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "dots",
      "label": "Dots",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "infinite",
      "label": "Infinite",
      "default": true
    },
    {
      "type": "header",
      "content": "Section Padding "
    },
    {
      "type": "text",
      "id": "paddingsection",
      "label": "Padding Section",
      "default": "0px 0px 0px 0px"
    },
    {
      "type": "text",
      "id": "paddingmobile",
      "label": "Padding Section Mobile",
      "default": "40px 0px 40px 0px"
    },
    {
      "type": "header",
      "content": "Section Margin "
    },
    {
      "type": "text",
      "id": "marginsection",
      "label": "Margin Section",
      "default": "0px 0px 0px 0px"
    },
    {
      "type": "text",
      "id": "marginmobile",
      "label": "Margin Mobile",
      "default": "0px 0px 0px 0px"
    }
  ],
  "blocks": [
    {
      "type": "collection",
      "name": "Collection",
      "settings": [
        {
          "id": "collection",
          "type": "collection",
          "label": "Collection"
        },
        {
          "id": "image",
          "type": "image_picker",
          "label": "Image"
        },
        {
          "id": "bg_title",
          "type": "image_picker",
          "label": "Background title"
        },
        {
          "id": "icon",
          "type": "textarea",
          "label": "Icon"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Collection list",
      "category": "Collection",
      "blocks": [
        { "type": "collection" },
        { "type": "collection" },
        { "type": "collection" }
      ]
    }
  ]
}
{% endschema %}